name: CI

on:
  push:
    branches:
      - main
    paths:
      - 'app/**'
      - 'manifests/**'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ghcr.io/fveim/devopsproject

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: ./app
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:${{ github.sha }}

      - name: Update image tag in dev overlay
        run: |
          sed -i "s/newTag: .*/newTag: ${{ github.sha }}/" manifests/overlays/dev/kustomization.yaml

      - name: Commit and push updated manifest
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add manifests/overlays/dev/kustomization.yaml
          git diff --staged --quiet || git commit -m "ci: update dev image tag to ${{ github.sha }}"
          git push
